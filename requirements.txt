# --- Core Streamlit app ---
streamlit>=1.30.0
pandas>=2.0.0

# --- HTTP client (eClass CDP, Ollama detection, generic URL fetches) ---
requests>=2.28.0

# --- Semantic matching stack (used by mapping_module) ---
# sentence-transformers pulls in torch; the app gracefully falls back to a
# text-based similarity if these are missing, but for a proper demo we keep
# them here so Streamlit Cloud produces the same scores as local runs.
sentence-transformers>=2.2.2
scikit-learn>=1.3.0

# --- Support-file parsers used by DocumentLibrary in enrichment_module ---
beautifulsoup4>=4.12.0
lxml>=4.9.0
PyPDF2>=3.0.0
python-docx>=1.1.0
